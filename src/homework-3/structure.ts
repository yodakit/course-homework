import { StructureItem } from './types'

export default class Structure {

  constructor(elements: StructureItem[]) {

  }
  
  get(key: string) {
    
  }
  
  set(key: string, value: unknown) {

  }
}